PORT=5000
HOST=0.0.0.0
DATABASE_URL=postgresql://user:password@localhost:5432/finance_db
# Render Postgres: paste Internal Database URL from the dashboard. If connections fail, ensure the URL includes
# ?sslmode=require (Render’s copied URL usually already does).
JWT_SECRET=your_super_secret_jwt_key

# Render.com sets RENDER_EXTERNAL_URL automatically. Optional: override the Swagger "Servers" URL.
# API_PUBLIC_URL=https://your-service.onrender.com
